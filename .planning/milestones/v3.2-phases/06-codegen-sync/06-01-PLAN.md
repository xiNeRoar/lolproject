---
phase: 06-codegen-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/api-client-react/src/generated/api.ts
  - lib/api-client-react/src/generated/api.schemas.ts
  - lib/api-zod/src/generated/api.ts
  - lib/api-zod/src/generated/types/
autonomous: true
requirements: [STAT-03]

must_haves:
  truths:
    - "Generated React Query hooks include getPlayerTeamStats function for the new career stats endpoint"
    - "Generated TypeScript types include hasPuuid and rsoOptIn in AuthMeResponse"
    - "Generated Zod validators include PlayerTeamStats schema with 12 properties"
    - "Zero TypeScript compilation errors in both generated packages"
  artifacts:
    - path: "lib/api-client-react/src/generated/api.ts"
      provides: "React Query hooks for all OpenAPI endpoints"
      contains: "getPlayerTeamStats"
    - path: "lib/api-client-react/src/generated/api.schemas.ts"
      provides: "TypeScript types for all OpenAPI schemas"
      contains: "hasPuuid"
    - path: "lib/api-zod/src/generated/api.ts"
      provides: "Zod validation schemas for all OpenAPI schemas"
      contains: "PlayerTeamStats"
  key_links:
    - from: "lib/api-spec/openapi.yaml"
      to: "lib/api-client-react/src/generated/"
      via: "Orval codegen (api-client-react target)"
      pattern: "orval --config ./orval.config.ts"
    - from: "lib/api-spec/openapi.yaml"
      to: "lib/api-zod/src/generated/"
      via: "Orval codegen (zod target)"
      pattern: "orval --config ./orval.config.ts"
---

<objective>
Regenerate frontend React Query hooks and Zod validators from the updated OpenAPI spec so all Phase 5 additions (AuthMeResponse with hasPuuid/rsoOptIn, PlayerTeamStats schema, getPlayerTeamStats endpoint) are available for frontend consumption.

Purpose: Phase 5 added new schemas and paths to openapi.yaml but never ran codegen. The generated files in lib/api-client-react and lib/api-zod are stale. Frontend cannot use hasPuuid or the career stats endpoint until codegen runs.
Output: Updated generated files in both packages, zero TypeScript errors, all Phase 5 additions present in generated output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-codegen-sync/06-CONTEXT.md
@lib/api-spec/orval.config.ts
@lib/api-spec/openapi.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Orval codegen to regenerate hooks and validators</name>
  <read_first>
    - lib/api-spec/orval.config.ts (codegen config — two targets: api-client-react and zod)
    - lib/api-spec/package.json (codegen script definition: "codegen": "orval --config ./orval.config.ts")
    - lib/api-spec/openapi.yaml (input spec — confirm AuthMeResponse, PlayerTeamStats, /players/{id}/team-stats exist)
  </read_first>
  <files>
    lib/api-client-react/src/generated/api.ts,
    lib/api-client-react/src/generated/api.schemas.ts,
    lib/api-zod/src/generated/api.ts,
    lib/api-zod/src/generated/types/
  </files>
  <action>
    Run the Orval codegen command from the api-spec directory (per D-01):

    ```bash
    cd lib/api-spec && pnpm run codegen
    ```

    This single command regenerates BOTH targets defined in orval.config.ts:
    1. api-client-react: React Query hooks at lib/api-client-react/src/generated/ (mode: "split", client: "react-query", clean: true)
    2. zod: Zod validators at lib/api-zod/src/generated/ (mode: "split", client: "zod", clean: true)

    The `clean: true` option in both targets wipes old output before generating, so stale files are automatically removed.

    Per D-02: Do NOT manually edit any generated files. If the output is wrong, the fix goes in openapi.yaml and codegen re-runs.
  </action>
  <verify>
    <automated>cd lib/api-spec && pnpm run codegen 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Orval codegen completes without errors (exit code 0)
    - lib/api-client-react/src/generated/api.ts exists and is non-empty
    - lib/api-client-react/src/generated/api.schemas.ts exists and is non-empty
    - lib/api-zod/src/generated/api.ts exists and is non-empty
    - lib/api-zod/src/generated/types/ directory contains .ts files
  </acceptance_criteria>
  <done>Orval codegen ran successfully, both generated output directories populated with fresh files</done>
</task>

<task type="auto">
  <name>Task 2: Verify generated output contains all Phase 5 additions and compiles</name>
  <read_first>
    - lib/api-client-react/src/generated/api.schemas.ts (check AuthMeResponse type has hasPuuid and rsoOptIn)
    - lib/api-client-react/src/generated/api.ts (check getPlayerTeamStats hook function exists)
    - lib/api-zod/src/generated/api.ts (check PlayerTeamStats Zod schema exists)
  </read_first>
  <files>None (verification only, no files modified)</files>
  <action>
    Run the full verification suite per D-03. All six checks must pass:

    1. TypeScript compilation — React Query hooks:
       ```bash
       pnpm --filter @workspace/api-client-react exec tsc --noEmit
       ```

    2. TypeScript compilation — Zod validators:
       ```bash
       pnpm --filter @workspace/api-zod exec tsc --noEmit
       ```

    3. Grep for hasPuuid in generated hooks types:
       ```bash
       grep -n "hasPuuid" lib/api-client-react/src/generated/api.schemas.ts
       ```
       Expected: Line containing `hasPuuid` property in AuthMeResponse type

    4. Grep for rsoOptIn in generated hooks types:
       ```bash
       grep -n "rsoOptIn" lib/api-client-react/src/generated/api.schemas.ts
       ```
       Expected: Line containing `rsoOptIn` property in AuthMeResponse type

    5. Grep for PlayerTeamStats in generated Zod validators:
       ```bash
       grep -rn "PlayerTeamStats" lib/api-zod/src/generated/
       ```
       Expected: Zod schema definition for PlayerTeamStats

    6. Grep for getPlayerTeamStats in generated hooks:
       ```bash
       grep -n "getPlayerTeamStats" lib/api-client-react/src/generated/api.ts
       ```
       Expected: React Query hook function for the /players/{id}/team-stats endpoint

    If any check fails, the OpenAPI spec is incorrect — read the relevant section of openapi.yaml to diagnose, fix the spec, and re-run codegen (per D-02).
  </action>
  <verify>
    <automated>pnpm --filter @workspace/api-client-react exec tsc --noEmit 2>&1 && pnpm --filter @workspace/api-zod exec tsc --noEmit 2>&1 && echo "TSC_PASS" || echo "TSC_FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @workspace/api-client-react exec tsc --noEmit` exits 0 (zero compile errors)
    - `pnpm --filter @workspace/api-zod exec tsc --noEmit` exits 0 (zero compile errors)
    - `grep "hasPuuid" lib/api-client-react/src/generated/api.schemas.ts` returns at least 1 match
    - `grep "rsoOptIn" lib/api-client-react/src/generated/api.schemas.ts` returns at least 1 match
    - `grep "PlayerTeamStats" lib/api-zod/src/generated/api.ts` returns at least 1 match
    - `grep "getPlayerTeamStats" lib/api-client-react/src/generated/api.ts` returns at least 1 match
  </acceptance_criteria>
  <done>All 6 verification checks pass: TypeScript compiles cleanly for both packages, hasPuuid and rsoOptIn present in AuthMeResponse types, PlayerTeamStats Zod schema exists, getPlayerTeamStats hook exists</done>
</task>

</tasks>

<verification>
Phase-level verification after all tasks complete:

1. `pnpm --filter @workspace/api-client-react exec tsc --noEmit` — zero errors
2. `pnpm --filter @workspace/api-zod exec tsc --noEmit` — zero errors
3. `grep "hasPuuid" lib/api-client-react/src/generated/api.schemas.ts` — present
4. `grep "rsoOptIn" lib/api-client-react/src/generated/api.schemas.ts` — present
5. `grep "PlayerTeamStats" lib/api-zod/src/generated/api.ts` — present
6. `grep "getPlayerTeamStats" lib/api-client-react/src/generated/api.ts` — present
</verification>

<success_criteria>
- Generated React Query hooks compile with zero TypeScript errors
- Generated Zod validators compile with zero TypeScript errors
- AuthMeResponse type includes hasPuuid boolean and rsoOptIn boolean
- PlayerTeamStats schema type exists in generated output
- getPlayerTeamStats hook function exists in generated React Query hooks
- STAT-03 requirement satisfied: codegen regenerated, frontend hooks updated
</success_criteria>

<output>
After completion, create `.planning/phases/06-codegen-sync/06-01-SUMMARY.md`
</output>
