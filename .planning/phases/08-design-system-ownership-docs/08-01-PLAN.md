---
phase: 08-design-system-ownership-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DESIGN_GUIDE.md
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "docs/DESIGN_GUIDE.md exists and contains all 18 color tokens extracted from index.css"
    - "Design guide documents Inter + Outfit font pairing with weight scales and Tailwind classes"
    - "Design guide includes 5-8 do/don't rules as guardrails for future frontend work"
    - "Design guide specifies three new patterns: per-team career card, activity heatmap, shareable card"
    - "Design guide contains bot-web alignment section with hex-to-token mapping table"
    - "Design guide documents layout patterns for both PublicLayout and AdminLayout paradigms"
  artifacts:
    - path: "docs/DESIGN_GUIDE.md"
      provides: "Canonical design system reference for all frontend work"
      contains: "--background"
  key_links:
    - from: "docs/DESIGN_GUIDE.md"
      to: "artifacts/vclol/src/index.css"
      via: "Token values extracted from CSS custom properties"
      pattern: "222 15% 6%"
    - from: "docs/DESIGN_GUIDE.md"
      to: "artifacts/discord-bot/src/lib/scoreboardRenderer.ts"
      via: "Bot hex values mapped to web tokens"
      pattern: "#0E1015"
---

<objective>
Create docs/DESIGN_GUIDE.md -- the canonical design system reference for all future VCLoL frontend work.

Purpose: The codebase has an implicit design system (color tokens in index.css, font pairing, 55 shadcn components, layout paradigms) that is undocumented. This guide formalizes it so Claude sessions implementing frontend features produce visually consistent output without re-reading every component file.

Output: docs/DESIGN_GUIDE.md with token reference tables, typography rules, layout patterns, component usage principles, do/don't guardrails, new pattern specifications, and bot-web alignment section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-design-system-ownership-docs/08-CONTEXT.md
@.planning/phases/08-design-system-ownership-docs/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/DESIGN_GUIDE.md with complete design system reference</name>
  <files>docs/DESIGN_GUIDE.md</files>
  <read_first>
    artifacts/vclol/src/index.css
    artifacts/vclol/src/components/layout/PublicLayout.tsx
    artifacts/vclol/src/components/layout/AdminLayout.tsx
    artifacts/discord-bot/src/lib/scoreboardRenderer.ts
    artifacts/discord-bot/src/lib/playerCardRenderer.ts
    artifacts/discord-bot/src/lib/teamCardRenderer.ts
    artifacts/discord-bot/src/lib/leaderboardRenderer.ts
    artifacts/vclol/src/lib/utils.ts
    artifacts/vclol/src/lib/lol-utils.ts
    .planning/phases/08-design-system-ownership-docs/08-RESEARCH.md
    .planning/phases/08-design-system-ownership-docs/08-CONTEXT.md
  </read_first>
  <action>
Create a new file `docs/DESIGN_GUIDE.md` with the following structure and content. Per D-01, this is a token reference + principles document, NOT per-component API documentation. Per D-02, include 5-8 do/don't rules. Per D-03 and D-04, include concept + data contract level specs for three new patterns. Per D-05, include bot embed alignment section with shared palette mapping.

**Document structure (per research recommendation):**

```
# VCLoL Design Guide

## Theme Overview
- Dark-only theme (Dark Charcoal and Steel Blue palette)
- No light mode -- never use `dark:` prefixed Tailwind classes
- Single dark palette resolved from CSS custom properties in `artifacts/vclol/src/index.css`

## Color Tokens
- Reference table with ALL 18 tokens from index.css
- Each row: Token name, HSL value, Hex equivalent, Role description
- Include these exact values from research:
  - `--background`: 222 15% 6%, #0E1015, Page background
  - `--foreground`: 210 20% 98%, #F8FAFC, Primary text
  - `--card`: 222 15% 9%, #151920, Card/panel background
  - `--card-foreground`: 210 20% 98%, #F8FAFC, Card text
  - `--popover`: 222 15% 9%, #151920, Popover background
  - `--popover-foreground`: 210 20% 98%, #F8FAFC, Popover text
  - `--primary`: 210 80% 55%, #2B8AEE, Brand accent (Steel Blue)
  - `--primary-foreground`: 0 0% 100%, #FFFFFF, Text on primary
  - `--secondary`: 222 15% 15%, #212838, Secondary surfaces
  - `--secondary-foreground`: 210 20% 98%, #F8FAFC, Text on secondary
  - `--muted`: 222 15% 12%, #1A1F28, Muted background
  - `--muted-foreground`: 215 15% 65%, #9BA3B0, Secondary text
  - `--accent`: 210 80% 55%, #2B8AEE, Same as primary
  - `--accent-foreground`: 0 0% 100%, #FFFFFF, Text on accent
  - `--destructive`: 0 84% 60%, #EF4444, Error/danger
  - `--destructive-foreground`: 210 40% 98%, #F8FAFC, Text on destructive
  - `--border`: 222 15% 18%, #272D36, Border color
  - `--input`: 222 15% 15%, #212838, Input background
  - `--ring`: 210 80% 55%, #2B8AEE, Focus ring
- Second table for radius tokens:
  - `--radius-sm`: 0.125rem (2px)
  - `--radius-md`: 0.25rem (4px)
  - `--radius-lg`: 0.5rem (8px)
  - `--radius` (base): 0.25rem (4px)
- Third table for semantic colors NOT in the token system (used directly via Tailwind):
  - green-400 / bg-green-400/20: Win indicators, success
  - red-400 / bg-red-400/20: Loss indicators, errors
  - yellow-400 / bg-yellow-400/20: Gold rank (ELO >= 1400)
  - purple-400 / bg-purple-400/20: Silver rank (ELO >= 1200)
  - blue-400 / bg-blue-400/20: Bronze rank (ELO >= 1100)

## Typography
- Font pairing table:
  - Inter: weights 400, 500, 600. Role: body text, labels, data, navigation. Tailwind: `font-sans` (default)
  - Outfit: weights 500, 600, 700, 800. Role: headings, display text, branding. Tailwind: `font-display`
- Typography conventions observed:
  - h1-h6 get `font-display` automatically via `@layer base` in index.css
  - `tracking-tight` applied to all headings globally
  - Card titles use `font-display` explicitly via CardTitle component
  - Navigation labels: `text-sm font-medium` (Inter)
  - Secondary text: always `text-muted-foreground` (never a custom gray)
  - Page titles: `text-4xl font-display font-bold`
  - Section headings: `text-xl font-semibold font-display`
  - Badge text: `text-xs` (Inter)

## Spacing and Radius
- No formal spacing scale defined -- document observed patterns:
  - Page content padding: `pt-16 pb-4` (standard) or `py-24` (hero sections)
  - Container max-width: `max-w-7xl` (public pages), unconstrained (admin)
  - Container horizontal padding: `px-4 sm:px-6 lg:px-8`
  - Card spacing: `space-y-3` for vertical stacks
- Radius from token table above
- Corner radius convention: use `rounded-lg` (8px) for cards, `rounded-md` (4px) for inputs/buttons

## Layout Patterns
- Public Layout section:
  - Sticky header: `backdrop-blur-md`, `bg-background/80`
  - Content container: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`
  - Footer: `border-t border-border/40 bg-card/30`
  - Mobile: hamburger menu at `md:` breakpoint
  - Navigation: horizontal `space-x-8` links at md+
- Admin Layout section:
  - Fixed sidebar `w-56` on md+, hidden on mobile
  - Sidebar section labels: uppercase `text-[10px] tracking-widest`
  - Content area: `overflow-y-auto p-8`
  - No max-width constraint
- Common page patterns:
  - Loading: `animate-pulse` skeleton cards
  - Empty state: dashed border container with Lucide icon (w-12 h-12) + message + CTA link
  - Card grids: `space-y-3` vertical stacking
  - Hero sections: full-width with gradient overlay

## Component Usage Principles
- Per D-01, document WHEN to use each component category, not HOW
- 55 shadcn/ui components available in `artifacts/vclol/src/components/ui/`
- Button variants table (6 variants, 4 sizes) -- from research Pattern 5
- Component selection guidance:
  - Card: content display, data panels, list items
  - Dialog: non-destructive modals (settings, details, forms)
  - AlertDialog: destructive confirmations (delete, remove, leave team)
  - Sheet: side panels, mobile navigation
  - Drawer: bottom-up mobile panels
  - Tabs: multi-view same-page content
  - Popover: contextual info on click
  - Tooltip: contextual info on hover
  - Toast/Sonner: transient notifications
- Utility: always use `cn()` from `@/lib/utils` for conditional classes
- Icons: Lucide React -- `w-4 h-4` for inline, `w-12 h-12` for empty states
- Animation: Framer Motion for page transitions. Pattern: `motion.div` with `initial={{ opacity: 0, y: 20 }}` and `animate={{ opacity: 1, y: 0 }}`
- Charts: Recharts with `hsl(var(--token))` for colors

## Do / Don't Rules
- Per D-02, include 5-8 rules. Use 7 rules from research Anti-Patterns section:
  1. Don't use raw color values -- always semantic tokens. Exception: win/loss colors (green-400, red-400) which lack dedicated tokens
  2. Don't write custom button styles -- use shadcn Button with correct variant
  3. Don't use Alert for confirmations -- use AlertDialog (destructive) or Dialog (non-destructive)
  4. Don't skip the container pattern -- public pages use `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`
  5. Don't use custom fonts -- only Inter (body) and Outfit (display)
  6. Don't build custom form inputs -- use shadcn form components
  7. Don't use light mode classes -- dark-only app, never add `dark:` prefixes

## New Pattern Specifications
- Per D-03 and D-04, concept + data contract level for each

### Per-Team Career Card
- Purpose: Show a player's record for one specific team in career history
- Data source: Per-team stats endpoint (Phase 5, STAT-01)
- Data shape (TypeScript interface):
  ```typescript
  {
    teamId: number;
    teamName: string;
    teamTag: string;
    role: string | null;
    status: string;
    wins: number;
    losses: number;
    avgKills: number;
    avgDeaths: number;
    avgAssists: number;
  }
  ```
- Layout: Card component, horizontal stack. Team name + tag left-aligned, role Badge if present, W/L record and KDA right-aligned. `text-muted-foreground` for secondary text. Inactive teams get muted treatment.
- Components: Card, Badge (for role), CardContent

### Activity Heatmap
- Purpose: Match frequency over time, GitHub contribution graph style
- Data source: Player match history dates (derive client-side from match listing)
- Data shape: `Record<string, number>` (ISO date string -> match count)
- Layout: Grid of 52 columns (weeks) x 7 rows (days). Small squares with primary color opacity scale:
  - 0 matches: `bg-muted`
  - 1 match: `bg-primary/20`
  - 2 matches: `bg-primary/40`
  - 3 matches: `bg-primary/60`
  - 4+ matches: `bg-primary/80` or `bg-primary`
- Month labels above, day labels (Mon/Wed/Fri) on left
- Components: Pure div grid (no shadcn component), Tooltip on hover for date + count

### Shareable Card
- Purpose: Compact card view optimized for screenshots
- Data source: Same data as current page (player profile, team profile, match result)
- Layout: Fixed-width container (480px), padded, dark background `--background`, VCLoL branding in corner, key stats prominent, no interactive elements
- Components: Card with custom compact layout, rendered as client-side overlay/modal
- NOT in scope: OG image generation, social media meta tags, server-side PNG rendering (deferred per CONTEXT.md)

## Bot Embed Alignment
- Per D-05, principles only -- no bot code changes
- Document the shared color palette mapping table (from research Bot-Web Color Alignment section):
  | Bot Constant | Hex | Web Token |
  |---|---|---|
  | bg/background | #0E1015 | --background |
  | card | #151920 | --card |
  | cardL/cardLight | #1C2230 | (no token -- lighter card for alternating rows) |
  | pri/primary | #2B8AEE | --primary |
  | fg/foreground | #F8FAFC | --foreground |
  | mut/muted | #9BA3B0 | --muted-foreground |
  | bdr/border | #272D36 | --border |
  | grn/green | #4ADE80 | green-400 |
  | red | #F87171 | red-400 |
  | blue | #3B82F6 | blue-500 |
  | redSide | #EF4444 | red-500 |
  | gold | #FACC15 | yellow-400 |
  | silver | #C0C0C0 | (standard silver) |
  | bronze | #CD7F32 | (standard bronze) |
  | purp | #A78BFA | violet-400 |
  | blu | #60A5FA | blue-400 |
- Font alignment: Bot uses Inter + Outfit same as web
- Structural alignment: 8px card radius, alternating row backgrounds, VCLoL branding
- Principle: When adding new web components that have bot equivalents, match the hex values from this table

## Canonical Source Reference
- Design tokens source of truth: `artifacts/vclol/src/index.css`
- Component library: `artifacts/vclol/src/components/ui/` (55 shadcn/ui components)
- Layout components: `artifacts/vclol/src/components/layout/`
- Bot renderers: `artifacts/discord-bot/src/lib/*Renderer.ts`
- Utility: `artifacts/vclol/src/lib/utils.ts` (cn function)
- ELO colors: `artifacts/vclol/src/lib/lol-utils.ts` (eloBadgeColor)
```

IMPORTANT CONSTRAINTS:
- Per D-01: Do NOT write per-component usage examples (shadcn docs cover that)
- Per D-03: Do NOT produce wireframes or pixel-level specs for new patterns
- Per research Pitfall 1: Guide should NOT exceed ~300 lines. If it's getting long, trim component section
- Per research Pitfall 2: Only document tokens that actually exist in index.css. Do NOT invent spacing scales or z-index hierarchies
- Per research Pitfall 5: Shareable card spec must NOT mention @napi-rs/canvas, og:image, or server-side rendering

Verify all token values against the actual index.css file read in read_first. If any value differs from what the research says, use the value from the actual file (source of truth).
  </action>
  <verify>
    <automated>grep -c "\-\-background" docs/DESIGN_GUIDE.md &amp;&amp; grep -c "\-\-primary" docs/DESIGN_GUIDE.md &amp;&amp; grep -c "Inter" docs/DESIGN_GUIDE.md &amp;&amp; grep -c "Outfit" docs/DESIGN_GUIDE.md &amp;&amp; grep -c "Don't" docs/DESIGN_GUIDE.md &amp;&amp; grep -c "Career Card" docs/DESIGN_GUIDE.md &amp;&amp; grep -c "Heatmap" docs/DESIGN_GUIDE.md &amp;&amp; grep -c "Shareable" docs/DESIGN_GUIDE.md &amp;&amp; grep -c "#0E1015" docs/DESIGN_GUIDE.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/DESIGN_GUIDE.md exists
    - File contains "# VCLoL Design Guide" as the document title
    - File contains a "## Color Tokens" section with all 18 token names (--background through --ring)
    - File contains HSL value "222 15% 6%" for --background
    - File contains hex "#2B8AEE" for --primary
    - File contains a "## Typography" section with both "Inter" and "Outfit" font names
    - File contains "font-display" and "font-sans" Tailwind class references
    - File contains a "## Layout Patterns" section with "max-w-7xl" container width
    - File contains "PublicLayout" and "AdminLayout" references
    - File contains a "## Do" section (or "Don't") with at least 5 rules
    - File contains "Don't use raw color values" or equivalent first rule
    - File contains "Don't use light mode classes" or equivalent last rule
    - File contains "### Per-Team Career Card" section with TypeScript data shape
    - File contains "### Activity Heatmap" section with "bg-primary/20" opacity reference
    - File contains "### Shareable Card" section WITHOUT any mention of "og:image" or "@napi-rs/canvas"
    - File contains "## Bot Embed Alignment" section with "#0E1015" hex value
    - File contains bot constant mapping with at least "scoreboardRenderer" or "Bot Constant" column header
    - File contains "## Canonical Source Reference" section referencing "index.css"
    - File does NOT contain per-component usage examples (no `<Button variant=` code blocks)
    - File is under 350 lines total (per research Pitfall 1 guidance of ~300 lines)
  </acceptance_criteria>
  <done>docs/DESIGN_GUIDE.md exists with complete design system reference covering all 18 color tokens, typography (Inter + Outfit), layout patterns (Public + Admin), 7 do/don't rules, 3 new pattern specs (career card, heatmap, shareable card), bot-web alignment mapping, and canonical source references. File stays under 350 lines and contains no per-component API docs.</done>
</task>

</tasks>

<verification>
- docs/DESIGN_GUIDE.md exists and is a valid markdown file
- All 18 CSS custom property tokens from index.css are documented with correct HSL and hex values
- Typography section covers both Inter and Outfit with correct weight ranges
- At least 5 do/don't rules present (target: 7)
- All three new patterns specified: per-team career card, activity heatmap, shareable card
- Bot-web alignment table maps all 16 bot color constants to web token equivalents
- No deferred features appear (OG image generation, bot code changes, CLAUDE.md restructuring)
- Document does not exceed 350 lines
</verification>

<success_criteria>
docs/DESIGN_GUIDE.md is a complete, accurate design system reference that a Claude session can read before implementing any frontend feature and produce visually consistent output without needing to scan the 55 component files or index.css directly.
</success_criteria>

<output>
After completion, create `.planning/phases/08-design-system-ownership-docs/08-01-SUMMARY.md`
</output>
