---
phase: 08-design-system-ownership-docs
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - CLAUDE.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "CLAUDE.md ownership section shows Claude owns artifacts/vclol/src/ (not Replit)"
    - "CLAUDE.md references docs/DESIGN_GUIDE.md as canonical design source"
    - "PROJECT.md context section reflects single AI agent (Claude full-stack), not two agents"
    - "PROJECT.md constraints section removes the old ownership boundary restricting Claude from frontend"
    - "PROJECT.md active requirements no longer attribute items to Replit"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated ownership boundary"
      contains: "artifacts/vclol/src/"
    - path: ".planning/PROJECT.md"
      provides: "Updated project context reflecting full-stack Claude"
      contains: "Claude owns full stack"
  key_links:
    - from: "CLAUDE.md"
      to: "docs/DESIGN_GUIDE.md"
      via: "Reference as canonical design source"
      pattern: "DESIGN_GUIDE.md"
    - from: ".planning/PROJECT.md"
      to: "CLAUDE.md"
      via: "Ownership model consistency"
      pattern: "full.stack"
---

<objective>
Update CLAUDE.md and PROJECT.md to reflect the ownership transition from split (Claude backend / Replit frontend) to full-stack Claude ownership.

Purpose: The ownership boundary has changed -- Claude now owns `artifacts/vclol/src/` (previously Replit). Both CLAUDE.md and PROJECT.md still reference the old split model. These updates ensure future sessions operate under the correct ownership model and reference the new DESIGN_GUIDE.md.

Output: Updated CLAUDE.md ownership section + updated PROJECT.md context, constraints, and active requirements sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-design-system-ownership-docs/08-CONTEXT.md
@.planning/phases/08-design-system-ownership-docs/08-RESEARCH.md
@.planning/phases/08-design-system-ownership-docs/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md ownership section (per D-06)</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md
    docs/DESIGN_GUIDE.md
    .planning/phases/08-design-system-ownership-docs/08-CONTEXT.md
  </read_first>
  <action>
Per D-06, make a MINIMAL update to CLAUDE.md. Only change the Ownership section and the Constraints reference. Do NOT restructure other sections or add frontend build commands.

**Change 1 -- Ownership section (currently lines 241-247):**

Replace the current text:
```
## Ownership

**Claude owns (Replit never edits):**
`lib/db/src/schema/` · `lib/api-spec/openapi.yaml` · `artifacts/api-server/` · `artifacts/discord-bot/` · `docs/` (except replit.md) · `CLAUDE.md`

**Replit owns (Claude never edits):**
`artifacts/vclol/src/` · `replit.md`
```

With:
```
## Ownership

**Claude owns:**
`lib/db/src/schema/` · `lib/api-spec/openapi.yaml` · `artifacts/api-server/` · `artifacts/discord-bot/` · `artifacts/vclol/src/` · `docs/` · `CLAUDE.md`

Design guide: `docs/DESIGN_GUIDE.md` is the canonical design reference for all frontend work.
```

**Change 2 -- Constraints section (in the "## Project" area near line 93-94):**

Find the line:
```
- **Ownership Boundary:** Claude never edits `artifacts/vclol/src/`, Replit never edits backend/bot/docs
```

Replace with:
```
- **Ownership:** Claude owns full stack (backend, bot, frontend, docs). See `docs/DESIGN_GUIDE.md` for frontend conventions.
```

NO other changes to CLAUDE.md. The diff should be under 15 lines total.
  </action>
  <verify>
    <automated>grep "Claude owns:" CLAUDE.md &amp;&amp; grep "artifacts/vclol/src/" CLAUDE.md &amp;&amp; grep "DESIGN_GUIDE.md" CLAUDE.md &amp;&amp; ! grep "Replit owns" CLAUDE.md &amp;&amp; ! grep "Claude never edits.*vclol" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains "**Claude owns:**" (not "Claude owns (Replit never edits):")
    - CLAUDE.md ownership section lists `artifacts/vclol/src/` in the Claude-owned paths
    - CLAUDE.md contains the string "DESIGN_GUIDE.md" as a reference
    - CLAUDE.md contains "canonical design reference" in the ownership section
    - CLAUDE.md does NOT contain "Replit owns (Claude never edits):" anywhere
    - CLAUDE.md does NOT contain "Claude never edits `artifacts/vclol/src/`" anywhere
    - CLAUDE.md constraints area contains "Claude owns full stack" or equivalent
    - CLAUDE.md diff is under 15 lines changed (no structural changes beyond ownership)
    - The rest of CLAUDE.md (schema, auth, data model, key constants, etc.) is completely unchanged
  </acceptance_criteria>
  <done>CLAUDE.md ownership section updated to show Claude owns artifacts/vclol/src/, references DESIGN_GUIDE.md as canonical design source, Replit ownership line removed, constraints updated. No other CLAUDE.md changes.</done>
</task>

<task type="auto">
  <name>Task 2: Update PROJECT.md for full-stack ownership (per D-07)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    .planning/PROJECT.md
    .planning/phases/08-design-system-ownership-docs/08-CONTEXT.md
    .planning/phases/08-design-system-ownership-docs/08-RESEARCH.md
  </read_first>
  <action>
Per D-07, update PROJECT.md in four specific locations to reflect full-stack Claude ownership.

**Change 1 -- Context section (currently line 82):**

Find:
```
- **Two AI agents:** Claude owns backend/bot/docs, Replit owns frontend (`artifacts/vclol/src/`)
```

Replace with:
```
- **Single AI agent:** Claude owns full stack (backend, bot, frontend, docs)
```

**Change 2 -- Constraints section (currently line 94):**

Find:
```
- **Ownership Boundary:** Claude never edits `artifacts/vclol/src/`, Replit never edits backend/bot/docs
```

Replace with:
```
- **Ownership:** Claude owns full stack. Frontend follows `docs/DESIGN_GUIDE.md` conventions.
```

**Change 3 -- Active requirements (currently lines 56-58):**

Find these three lines containing "(Replit)":
```
- [ ] Website RSO OAuth handler (/connect page + /auth/rso callback) — launch blocker (Replit)
- [ ] Player profile career resume layout (per-team W/L + KDA, remove ELO trajectory) — (Replit, depends on v3.2 backend)
- [ ] Login flow RSO connect step — (Replit, depends on v3.2 /auth/me)
```

Replace the attributions:
```
- [ ] Website RSO OAuth handler (/connect page + /auth/rso callback) — launch blocker
- [ ] Player profile career resume layout (per-team W/L + KDA, remove ELO trajectory) — depends on v3.2 backend
- [ ] Login flow RSO connect step — depends on v3.2 /auth/me
```

**Change 4 -- Key Decisions table (after the last row, before the closing `|`):**

Add a new row:
```
| Full-stack Claude ownership | Replit no longer active on project; Claude takes frontend ownership with design guide as canonical reference | -- Transition (v3.2) |
```

NO other changes to PROJECT.md. Update the "Last updated" line at the bottom to reflect the current date.
  </action>
  <verify>
    <automated>grep "Single AI agent" .planning/PROJECT.md &amp;&amp; grep "Claude owns full stack" .planning/PROJECT.md &amp;&amp; ! grep "Two AI agents" .planning/PROJECT.md &amp;&amp; ! grep "(Replit)" .planning/PROJECT.md &amp;&amp; grep "Full-stack Claude ownership" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - PROJECT.md contains "Single AI agent" in the Context section (not "Two AI agents")
    - PROJECT.md contains "Claude owns full stack" in the Constraints section
    - PROJECT.md does NOT contain "Two AI agents" anywhere
    - PROJECT.md does NOT contain "(Replit)" attribution on any active requirement line
    - PROJECT.md active requirements still list all three frontend items (RSO handler, career resume, login flow) but without "(Replit)" suffix
    - PROJECT.md Key Decisions table contains a row with "Full-stack Claude ownership"
    - PROJECT.md Constraints section references "DESIGN_GUIDE.md"
    - PROJECT.md "Last updated" line shows current date (2026-03-28)
    - All other PROJECT.md content (What This Is, Core Value, Validated requirements, Out of Scope, Evolution) is unchanged
  </acceptance_criteria>
  <done>PROJECT.md reflects full-stack Claude ownership: context says single AI agent, constraints reference design guide, active requirements drop Replit attribution, key decisions table records the transition.</done>
</task>

</tasks>

<verification>
- CLAUDE.md ownership section lists `artifacts/vclol/src/` as Claude-owned
- CLAUDE.md references DESIGN_GUIDE.md as canonical
- CLAUDE.md contains no "Replit owns" line
- PROJECT.md says "Single AI agent" not "Two AI agents"
- PROJECT.md constraints say "Claude owns full stack"
- PROJECT.md has no "(Replit)" attributions on active requirements
- PROJECT.md key decisions table includes ownership transition row
- Neither file has structural changes beyond the specified locations
</verification>

<success_criteria>
Both CLAUDE.md and PROJECT.md consistently reflect full-stack Claude ownership. Any future Claude session reading these files will understand it owns the frontend and should reference docs/DESIGN_GUIDE.md for design conventions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-design-system-ownership-docs/08-02-SUMMARY.md`
</output>
