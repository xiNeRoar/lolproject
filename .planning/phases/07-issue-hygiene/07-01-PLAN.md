---
phase: 07-issue-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [ISSUE-01, ISSUE-04]

must_haves:
  truths:
    - "Issue #198 body shows GET /auth/connect/:token (not POST /api/auth/connect)"
    - "Issue #198 body shows GET /auth/rso (not GET /api/auth/rso/authorize)"
    - "Issue #198 body shows GET /auth/rso/callback (not GET /api/auth/rso/callback with wrong OpenAPI operationId)"
    - "All 5 open Replit-labelled issues (#198, #199, #200, #202, #204) have claude label added and replit label removed"
    - "All 5 open Replit-labelled issues have [Replit] prefix changed to [Claude] in title"
  artifacts: []
  key_links: []
---

<objective>
Fix Issue #198 with correct API paths and transfer all open Replit-labelled issues to Claude ownership.

Purpose: Issue #198 references wrong API paths that would mislead frontend implementation. Replit no longer owns frontend -- Claude does (per Phase 8 ownership update). All open frontend issues must reflect this.
Output: #198 body corrected, 5 issues relabelled from replit to claude with titles updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

Note: This phase uses GitHub API operations exclusively -- no source code files are modified.
The project accesses GitHub via `gh` CLI (authenticated as xiNeRoar). The repo is xiNeRoar/lolproject.

CLAUDE.md documents a Python-based GitHub API approach using a token from git remote URL, but the remote URL does not contain an inline token. Use `gh` CLI instead, which is authenticated.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Issue #198 body with correct API paths</name>
  <files></files>
  <read_first>
    - lib/api-spec/openapi.yaml (lines 193-230 for /auth/connect/{token}, /auth/rso, /auth/rso/callback paths)
  </read_first>
  <action>
Update Issue #198 body to replace the 3 incorrect API references with the correct ones from OpenAPI spec:

**Current (wrong) paths in issue body:**
1. Step 1: `POST /api/auth/connect` (OpenAPI: `postAuthConnect`) -- WRONG verb and path
2. Step 3: `GET /api/auth/rso/authorize` (OpenAPI: `getRsoAuthorize`) -- WRONG path, no /authorize suffix
3. Step 4: `GET /api/auth/rso/callback` (OpenAPI: `getRsoCallback`) -- path correct but operationId may be wrong

**Correct paths from openapi.yaml (lines 193-217):**
1. `GET /auth/connect/{token}` -- validates connect token (GET with token in path, not POST)
2. `GET /auth/rso` -- initiates RSO OAuth flow (no /authorize suffix)
3. `GET /auth/rso/callback` -- RSO OAuth callback

Use `gh issue edit 198 --body "..."` with the corrected body. The full replacement body is:

```
**Reference:** PRD v3.1 section 6.1, section 11

**What:** New page at `/connect?token=abc123`. Bot `/connect` sends users here.

**Flow:**
1. Validate token via `GET /api/auth/connect/{token}` (OpenAPI: `getAuthConnectToken`)
2. Discord OAuth (auto, using discordId from token)
3. "Connect Riot Account" button -> `GET /api/auth/rso` (OpenAPI: `getAuthRso`)
4. Riot login -> redirect -> `GET /api/auth/rso/callback` (OpenAPI: `getAuthRsoCallback`)
5. Success: Discord + Riot avatars side-by-side + "Verified!"
6. Token expired: "This link has expired. Run `/connect` again in Discord."

**Acceptance:**
- [ ] /connect?token=valid -> completes RSO flow
- [ ] /connect?token=expired -> shows expiry message
- [ ] /connect (no token) -> shows general "Connect" page with Discord login first
```

Before writing, read openapi.yaml lines 193-230 to confirm exact operationId values. Use those exact operationId values in the parenthetical references.
  </action>
  <verify>
    <automated>cd "C:/Users/User/Desktop/VS Code Project/lolproject" && gh issue view 198 --json body --jq .body | grep -c "GET /api/auth/connect"</automated>
  </verify>
  <acceptance_criteria>
    - Issue #198 body contains "GET /api/auth/connect/{token}" (not "POST /api/auth/connect")
    - Issue #198 body contains "GET /api/auth/rso" without /authorize suffix
    - Issue #198 body contains "GET /api/auth/rso/callback"
    - All 3 OpenAPI operationId references match actual spec values
  </acceptance_criteria>
  <done>Issue #198 body shows correct API paths matching openapi.yaml lines 193-217</done>
</task>

<task type="auto">
  <name>Task 2: Relabel all open Replit issues to Claude ownership</name>
  <files></files>
  <read_first>
    - CLAUDE.md (ownership section confirming Claude owns artifacts/vclol/src/)
  </read_first>
  <action>
There are exactly 5 open issues with the "replit" label. For each one:
1. Remove the "replit" label
2. Add the "claude" label
3. Update the title: replace "[Replit]" prefix with "[Claude]"

The 5 issues and their new titles:
- #198: "[Claude] RSO Connect page (/connect?token=...) — launch blocker"
- #199: "[Claude] Match detail auth gate for scrim privacy — launch blocker"
- #200: "[Claude] Player profile — career resume layout + RSO opt-in gate"
- #202: "[Claude] Search — only show RSO opt-in players"
- #204: "[Claude] Login flow — add RSO connect step"

For each issue, run:
```bash
gh issue edit {NUMBER} --title "{NEW_TITLE}" --remove-label "replit" --add-label "claude"
```

After all 5 edits, verify by listing open issues with "replit" label (should return 0) and open issues with "claude" label (should include all 5).
  </action>
  <verify>
    <automated>cd "C:/Users/User/Desktop/VS Code Project/lolproject" && gh issue list --label replit --state open --json number --jq 'length'</automated>
  </verify>
  <acceptance_criteria>
    - Zero open issues have the "replit" label (gh issue list --label replit --state open returns empty)
    - Issues #198, #199, #200, #202, #204 all have the "claude" label
    - All 5 issue titles start with "[Claude]" instead of "[Replit]"
    - The "frontend" label is preserved on all 5 issues (only "replit" removed, "claude" added)
  </acceptance_criteria>
  <done>All 5 open Replit issues (#198, #199, #200, #202, #204) relabelled to Claude with updated titles</done>
</task>

</tasks>

<verification>
1. `gh issue view 198 --json body --jq .body` shows GET /api/auth/connect/{token}, GET /api/auth/rso, GET /api/auth/rso/callback
2. `gh issue list --label replit --state open` returns 0 results
3. `gh issue list --label claude --state open --json number,title` includes all 5 issues with [Claude] prefix
</verification>

<success_criteria>
- Issue #198 body contains all 3 correct API paths from openapi.yaml
- Zero open issues carry the "replit" label
- All 5 former Replit issues carry the "claude" label with updated titles
</success_criteria>

<output>
After completion, create `.planning/phases/07-issue-hygiene/07-01-SUMMARY.md`
</output>
