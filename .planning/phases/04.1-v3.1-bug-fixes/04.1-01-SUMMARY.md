---
phase: 04.1-v3.1-bug-fixes
plan: 01
subsystem: api
tags: [express, drizzle, privacy, elo, typescript, vods]

# Dependency graph
requires:
  - phase: 03-privacy-codegen
    provides: v3.1 privacy model, matchRecorder reference implementation
provides:
  - submitRofl visibility fallback matches bot matchRecorder ("private" default)
  - submitRofl explicit matchType "scrim" in INSERT
  - Dead ELO code removed from submitRofl (no eloEligible, calculateElo, ladderSettings)
  - vods PUT body type includes all destructured fields
  - RSO token schema comments corrected
  - REQUESTS.md cleaned of stale entries
affects: [api-server, schema, docs]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "submitRofl visibility logic matches matchRecorder 1:1 (2-way public/private, no default branch)"

key-files:
  created: []
  modified:
    - artifacts/api-server/src/routes/submitRofl.ts
    - artifacts/api-server/src/routes/vods.ts
    - lib/db/src/schema/players.ts
    - docs/REQUESTS.md

key-decisions:
  - "Removed dead ELO code entirely rather than leaving guarded -- scrims never compute ELO"
  - "RSO token columns kept but comments clarified as Tournament API reservation"

patterns-established:
  - "submitRofl and matchRecorder must use identical visibility fallback logic"

requirements-completed: ["#222", "#223", "#224"]

# Metrics
duration: 5min
completed: 2026-03-28
---

# Phase 04.1 Plan 01: v3.1 Bug Fixes Summary

**Fixed submitRofl visibility fallback (private default), added explicit matchType, removed 47 lines of dead ELO code, corrected vods PUT type and RSO schema comments**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-28T06:34:47Z
- **Completed:** 2026-03-28T06:39:43Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Fixed critical visibility bug in submitRofl.ts: fallback was "default" (7-day auto-public) instead of "private", violating PRD v3.1
- Added explicit `matchType: "scrim"` to submitRofl INSERT, matching bot matchRecorder behavior
- Removed 47 lines of dead ELO code (calculateElo, ladderSettings, eloHistoryTable, eloEligible guard) from submitRofl
- Fixed vods.ts PUT body type assertion to include gameNumber, vodType, teamId
- Corrected RSO token schema comments from misleading "encrypted, nullable" to "reserved for Tournament API, always null"
- Cleared stale REQUESTS.md entries for completed issues #217 and #221

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix submitRofl.ts -- visibility fallback, matchType, dead ELO code** - `b3a7c8f` (fix)
2. **Task 2: Fix vods.ts TypeScript errors, RSO schema comments, stale REQUESTS.md** - `9d72fbb` (fix)

## Files Created/Modified
- `artifacts/api-server/src/routes/submitRofl.ts` - Fixed visibility fallback to "private", added matchType: "scrim", removed dead ELO code
- `artifacts/api-server/src/routes/vods.ts` - Added missing gameNumber/vodType/teamId to PUT body type assertion
- `lib/db/src/schema/players.ts` - Corrected RSO token column comments
- `docs/REQUESTS.md` - Cleared stale entries for #217 and #221

## Decisions Made
- Removed dead ELO code entirely rather than leaving it guarded behind an always-false condition. The matchRecorder reference implementation demonstrates the correct pattern: scrims never compute ELO, only W/L.
- RSO token columns kept in schema (needed for future Tournament API) but comments corrected to avoid misleading "encrypted" language since tokens are never populated.

## Deviations from Plan

### Plan Accuracy Notes

The plan anticipated implicit-any TypeScript errors (TS7006) in vods.ts lambda parameters. The actual codebase had no such errors -- TypeScript properly infers types from the Drizzle query chain. The real bug was the incomplete PUT body type assertion missing three fields. This was addressed as planned.

None - plan executed as written with the above clarification on vods.ts error scope.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Known Stubs
None - all fixes are complete with no placeholder values.

## Next Phase Readiness
- All v3.1 code review bugs fixed, three GitHub issues (#222, #223, #224) closeable
- Ready to proceed with Phase 5 (v3.2 route + spec work)

---
*Phase: 04.1-v3.1-bug-fixes*
*Completed: 2026-03-28*
