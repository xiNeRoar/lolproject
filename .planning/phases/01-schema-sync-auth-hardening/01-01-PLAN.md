---
phase: 01-schema-sync-auth-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/SCHEMA_CONTRACT.md
  - lib/db/src/schema/eloHistory.ts
autonomous: true
requirements:
  - SCHM-01
  - SCHM-02

must_haves:
  truths:
    - "eloHistory table has no playerId column in code or documentation"
    - "SCHEMA_CONTRACT.md eloHistory section matches actual eloHistory.ts code exactly"
    - "drizzle-kit push reports zero pending changes against the database"
    - "FK relationship map in SCHEMA_CONTRACT.md has no elo_history.playerId entry"
  artifacts:
    - path: "docs/SCHEMA_CONTRACT.md"
      provides: "Corrected eloHistory definition without playerId"
      contains: "teamId: integer(\"team_id\").notNull()"
    - path: "lib/db/src/schema/eloHistory.ts"
      provides: "Source of truth for eloHistory schema"
      contains: "teamId"
  key_links:
    - from: "docs/SCHEMA_CONTRACT.md"
      to: "lib/db/src/schema/eloHistory.ts"
      via: "Manual sync — doc must match code"
      pattern: "teamId.*team_id.*notNull"
---

<objective>
Sync the production database schema with Drizzle ORM definitions and fix the stale eloHistory documentation.

Purpose: SCHM-01 ensures all columns exist in production (match_type, tournament_code, etc.). SCHM-02 resolves the eloHistory.playerId discrepancy between docs and code. Both are prerequisites for all subsequent work.
Output: Updated SCHEMA_CONTRACT.md, verified database schema sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/SCHEMA_CONTRACT.md
@lib/db/src/schema/eloHistory.ts
@docs/REQUESTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SCHEMA_CONTRACT.md eloHistory section and FK map (SCHM-02, per D-01)</name>
  <files>docs/SCHEMA_CONTRACT.md</files>
  <read_first>
    - docs/SCHEMA_CONTRACT.md (current stale doc — has playerId on line ~167)
    - lib/db/src/schema/eloHistory.ts (actual code — source of truth, has NO playerId)
  </read_first>
  <action>
Edit docs/SCHEMA_CONTRACT.md to match the actual eloHistory.ts code (per D-01 locked decision):

1. Replace the `eloHistory` section (currently around line 162-173) with the actual code from eloHistory.ts:
```typescript
### `eloHistory` (MODIFY -- team-only ELO, no playerId)
export const eloHistoryTable = pgTable("elo_history", {
  id: serial("id").primaryKey(),
  teamId: integer("team_id").notNull().references(() => teamsTable.id, { onDelete: "cascade" }),
  elo: integer("elo").notNull(),
  delta: integer("delta").notNull().default(0),
  matchId: integer("match_id").references(() => matchesTable.id, { onDelete: "set null" }),
  reason: text("reason").notNull().default("match"),
  createdAt: timestamp("created_at").defaultNow().notNull(),
});
```

Key changes from current doc:
- Remove the `playerId: integer("player_id").references(...)` line entirely
- Change `teamId` from optional `.references()` to `.notNull().references()` (matches actual code)
- Remove the comment "keep for backward compat" on playerId
- Change section header from "(MODIFY -- add teamId)" to "(MODIFY -- team-only ELO, no playerId)"

2. In the FK Relationship Map section (around line 325-326), remove the line:
```
elo_history.playerId → players.id
```
Keep only:
```
elo_history.teamId → teams.id
elo_history.matchId → matches.id
```

Do NOT change any other table definitions or sections.
  </action>
  <verify>
    <automated>grep -c "playerId" docs/SCHEMA_CONTRACT.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - docs/SCHEMA_CONTRACT.md eloHistory section contains `teamId: integer("team_id").notNull()`
    - docs/SCHEMA_CONTRACT.md eloHistory section does NOT contain any line with `playerId` or `player_id`
    - FK Relationship Map has `elo_history.teamId` line but no `elo_history.playerId` line
    - No other table definitions in the file were modified
  </acceptance_criteria>
  <done>SCHEMA_CONTRACT.md eloHistory matches actual eloHistory.ts code. No playerId in eloHistory anywhere in the doc's definition or FK map.</done>
</task>

<task type="auto">
  <name>Task 2: Run drizzle-kit push to sync all missing columns (SCHM-01)</name>
  <files>lib/db/src/schema/eloHistory.ts</files>
  <read_first>
    - lib/db/drizzle.config.ts (migration config — connection string, schema path)
    - docs/REQUESTS.md (lists known missing columns: match_type, tournament_code)
    - docs/DEPLOYMENT.md (schema change workflow: push for dev, generate+migrate for production)
  </read_first>
  <action>
Run drizzle-kit push against the development database to identify and apply all missing columns.

1. First, verify the current schema diff:
```bash
cd lib/db && pnpm drizzle-kit push --dry-run 2>&1 || echo "dry-run not supported, proceed with push"
```

2. If dry-run is not available, run the actual push:
```bash
cd lib/db && pnpm run generate
```
This generates migration SQL files showing exactly what columns are missing.

3. Review the generated migration to confirm it includes:
   - `match_type` column on matches table
   - `tournament_code` column on matches table
   - Any other missing columns across all tables

4. IMPORTANT per research pitfall: If drizzle-kit prompts to "rename" a column, ALWAYS select "create column" instead. Renaming drops existing data.

5. For production, the workflow is generate + migrate (per D-02 and DEPLOYMENT.md). For local dev, push is acceptable.

Note: If no local database is available for the executor, generate the migration SQL and document the pending changes. The schema files themselves are already correct in code — the gap is database-level only.
  </action>
  <verify>
    <automated>cd "lib/db" && pnpm drizzle-kit generate 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Running `pnpm drizzle-kit generate` in lib/db produces a migration file OR reports "No schema changes" (meaning schema is already synced)
    - If a migration file is generated, it contains ALTER TABLE statements for missing columns (match_type, tournament_code at minimum)
    - No DROP COLUMN statements in the migration (safety check)
  </acceptance_criteria>
  <done>All columns defined in Drizzle schema files exist in the database (or a migration file has been generated to apply them). drizzle-kit reports zero pending schema differences.</done>
</task>

</tasks>

<verification>
- `grep -c "player_id" docs/SCHEMA_CONTRACT.md` shows the playerId count decreased (eloHistory entry removed)
- `grep "elo_history.playerId" docs/SCHEMA_CONTRACT.md` returns no matches
- `grep "elo_history.teamId" docs/SCHEMA_CONTRACT.md` returns a match
- Schema generation produces no unexpected DROP statements
</verification>

<success_criteria>
1. SCHEMA_CONTRACT.md eloHistory section exactly matches lib/db/src/schema/eloHistory.ts (no playerId)
2. FK map has no elo_history.playerId entry
3. Database schema sync status is known (migration generated or already synced)
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-sync-auth-hardening/01-01-SUMMARY.md`
</output>
