---
phase: 03-api-contract-performance
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - lib/api-spec/openapi.yaml
  - lib/api-client-react/src/generated/api.ts
  - lib/api-client-react/src/generated/api.schemas.ts
  - lib/api-zod/src/generated/api.ts
autonomous: true
requirements: [SPEC-01, SPEC-02]

must_haves:
  truths:
    - "OpenAPI spec documents all 7 auth endpoints with correct paths and methods"
    - "OpenAPI spec includes isRedacted, isPrivate, and _masked privacy fields"
    - "OpenAPI spec documents paginated response wrappers for players and matches"
    - "Running codegen produces zero type errors"
  artifacts:
    - path: "lib/api-spec/openapi.yaml"
      provides: "Complete API specification aligned with actual routes"
      contains: "/auth/discord"
    - path: "lib/api-client-react/src/generated/api.ts"
      provides: "Regenerated React Query hooks"
    - path: "lib/api-zod/src/generated/api.ts"
      provides: "Regenerated Zod validators"
  key_links:
    - from: "lib/api-spec/openapi.yaml"
      to: "lib/api-client-react/src/generated/"
      via: "Orval codegen"
      pattern: "operationId"
    - from: "lib/api-spec/openapi.yaml"
      to: "lib/api-zod/src/generated/"
      via: "Orval codegen"
      pattern: "operationId"
---

<objective>
Align the OpenAPI spec with all actual API routes -- including auth endpoints, Phase 2 privacy response fields, paginated response wrappers, and a unified ErrorResponse schema -- then run Orval codegen to regenerate frontend hooks and Zod validators.

Purpose: The spec must be the single source of truth for the API contract. After Phases 1-2 changed response shapes and this phase added pagination, the spec is significantly drifted. Per D-05, all spec changes are batched into one update, then codegen runs once.
Output: Updated `openapi.yaml`, regenerated `lib/api-client-react/` and `lib/api-zod/` generated files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api-contract-performance/03-CONTEXT.md
@.planning/phases/03-api-contract-performance/03-RESEARCH.md
@.planning/phases/03-api-contract-performance/03-01-SUMMARY.md
@.planning/phases/03-api-contract-performance/03-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

Actual auth routes (from artifacts/api-server/src/routes/auth.ts):
```
GET  /auth/discord           -> 302 redirect to Discord OAuth
GET  /auth/discord/callback  -> 302 redirect to /dashboard or /register (params: code, state)
GET  /auth/connect/:token    -> 302 redirect to RSO OAuth (path param: token)
GET  /auth/rso               -> 302 redirect to Riot RSO OAuth
GET  /auth/rso/callback      -> 302 redirect to / or error page (params: code, state)
GET  /auth/me                -> 200 JSON { playerId, riotId, discordId } or 401
POST /auth/logout             -> 200 JSON { ok: true } (destroys session)
```

Spec drift (from RESEARCH.md audit):
| Spec Path | Issue |
|-----------|-------|
| `/auth/rso/authorize` | Path mismatch -- should be `/auth/rso` |
| `/auth/connect` (POST) | Should be GET `/auth/connect/{token}` |
| `/auth/discord` | Missing entirely |
| `/auth/discord/callback` | Missing entirely |
| `/auth/logout` | Missing entirely |
| MatchDetail response | Missing `isRedacted` field |
| MatchPlayerEntry response | Missing `_masked` field |
| PlayerProfile response | Missing `isPrivate` variant |
| Player (list) | Flat array -> paginated wrapper |
| Match (list) | Flat array -> paginated wrapper |

Privacy response shapes from Phase 2 (privacyGate.ts):
- Redacted scrim match: `{ id, teamAId, teamBId, sideAName, sideBName, matchTitle, winnerName, score, isRedacted: true }`
- Masked match player: `{ ...normalFields, riotId: "Hidden", puuid: "Hidden", _masked: true }`
- Private player profile: `{ id, riotId, isPrivate: true, teams: [...] }`

Codegen command: `cd lib/api-spec && pnpm run codegen`
Orval config: `lib/api-spec/orval.config.ts`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full OpenAPI spec alignment (D-03, D-04)</name>
  <files>lib/api-spec/openapi.yaml</files>
  <read_first>
    lib/api-spec/openapi.yaml
    artifacts/api-server/src/routes/auth.ts
    artifacts/api-server/src/routes/matches.ts
    artifacts/api-server/src/routes/players.ts
    artifacts/api-server/src/routes/vods.ts
    artifacts/api-server/src/lib/privacyGate.ts
    artifacts/api-server/src/lib/formatters.ts
  </read_first>
  <action>
    Read the full `openapi.yaml` (3658 lines) and the actual route files, then make these specific changes:

    **1. Auth Endpoints (D-03) -- Replace/add in the `# Auth` section of paths:**

    Remove existing incorrect entries:
    - Delete `/auth/rso/authorize` (wrong path)
    - Delete `/auth/connect` POST (wrong method + path)

    Add all 7 auth endpoints with correct signatures:

    ```yaml
    /auth/discord:
      get:
        operationId: authDiscord
        tags: [auth]
        summary: Initiate Discord OAuth flow
        responses:
          "302":
            description: Redirect to discord.com/api/oauth2/authorize

    /auth/discord/callback:
      get:
        operationId: authDiscordCallback
        tags: [auth]
        summary: Discord OAuth callback
        parameters:
          - name: code
            in: query
            required: true
            schema:
              type: string
          - name: state
            in: query
            required: true
            schema:
              type: string
        responses:
          "302":
            description: Redirect to /dashboard (existing player) or /register (new)
          "403":
            description: Invalid OAuth state
            content:
              application/json:
                schema:
                  $ref: "#/components/schemas/ErrorResponse"

    /auth/connect/{token}:
      get:
        operationId: authConnectToken
        tags: [auth]
        summary: Bot /connect flow -- validate token and redirect to RSO
        parameters:
          - name: token
            in: path
            required: true
            schema:
              type: string
        responses:
          "302":
            description: Redirect to RSO OAuth or error page

    /auth/rso:
      get:
        operationId: authRso
        tags: [auth]
        summary: Initiate RSO OAuth flow (website-only)
        responses:
          "302":
            description: Redirect to auth.riotgames.com

    /auth/rso/callback:
      get:
        operationId: authRsoCallback
        tags: [auth]
        summary: RSO OAuth callback
        parameters:
          - name: code
            in: query
            required: true
            schema:
              type: string
          - name: state
            in: query
            required: true
            schema:
              type: string
        responses:
          "302":
            description: Redirect to / on success or error page on failure

    /auth/logout:
      post:
        operationId: authLogout
        tags: [auth]
        summary: Destroy player session
        responses:
          "200":
            description: Logged out
            content:
              application/json:
                schema:
                  type: object
                  properties:
                    ok:
                      type: boolean
    ```

    Keep the existing `/auth/me` endpoint as-is (it is correct).

    **2. ErrorResponse Schema (D-04) -- Add to components/schemas:**
    ```yaml
    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: string
    ```
    Then update all existing `4xx`/`5xx` responses throughout the spec to reference `$ref: "#/components/schemas/ErrorResponse"` where they currently use inline error schemas. At minimum, add ErrorResponse references to the newly added auth endpoints above.

    **3. Privacy Response Fields (D-03) -- Update existing schemas:**

    In the `MatchDetail` or `Match` schema (find the schema that represents a match detail response), add:
    ```yaml
    isRedacted:
      type: boolean
      description: "true when scrim match is redacted for non-participants"
    ```

    In the `MatchPlayerEntry` or match player schema, add:
    ```yaml
    _masked:
      type: boolean
      description: "true when player has not opted in via RSO"
    ```

    Find the `PlayerProfile` or player detail schema and add an `isPrivate` variant. This can be done via a `oneOf` or by adding `isPrivate` as an optional boolean:
    ```yaml
    isPrivate:
      type: boolean
      description: "true when player profile is private (only id, riotId, teams returned)"
    ```

    **4. Paginated Response Wrappers (D-02, D-08) -- Update list endpoints:**

    For GET `/players` response: Change from returning an array of players to:
    ```yaml
    PaginatedPlayers:
      type: object
      required: [data, total, page, totalPages]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/PlayerListItem"
        total:
          type: integer
        page:
          type: integer
        totalPages:
          type: integer
    ```
    Update the GET `/players` response to use `PaginatedPlayers`.
    Add `page` and `limit` query parameters to GET `/players`.

    For GET `/matches` response: Change from returning an array of matches to:
    ```yaml
    PaginatedMatches:
      type: object
      required: [data, total, page, totalPages]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/MatchListItem"
        total:
          type: integer
        page:
          type: integer
        totalPages:
          type: integer
    ```
    Update the GET `/matches` response to use `PaginatedMatches`.
    Add `page` and `limit` query parameters to GET `/matches`.

    If `PlayerListItem` or `MatchListItem` schemas don't exist, create them based on the actual response shape from the route handlers. For MatchListItem, reference the formatMatch superset fields from `formatters.ts` plus `vodCount: integer`. For PlayerListItem, include formatPlayer fields plus `primaryTeam` (nullable object with teamId, teamName, teamTag), `totalGames` (integer), `winRate` (nullable integer).

    **5. Verify existing spec correctness:**
    Cross-reference `artifacts/api-server/src/routes/index.ts` to confirm all mounted routes are documented. Focus on auth routes -- the other routes (teams, matches, players, events, etc.) should already be mostly correct except for the response shape changes noted above.
  </action>
  <verify>
    <automated>grep -c "/auth/discord" lib/api-spec/openapi.yaml && grep -c "/auth/logout" lib/api-spec/openapi.yaml && grep -c "ErrorResponse" lib/api-spec/openapi.yaml && grep -c "isRedacted" lib/api-spec/openapi.yaml && grep -c "PaginatedPlayers" lib/api-spec/openapi.yaml && grep -c "PaginatedMatches" lib/api-spec/openapi.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `openapi.yaml` contains `/auth/discord:` path with GET method (operationId: authDiscord)
    - `openapi.yaml` contains `/auth/discord/callback:` path with GET method
    - `openapi.yaml` contains `/auth/connect/{token}:` path with GET method (not POST `/auth/connect`)
    - `openapi.yaml` contains `/auth/rso:` path with GET method (not `/auth/rso/authorize`)
    - `openapi.yaml` contains `/auth/rso/callback:` path
    - `openapi.yaml` contains `/auth/logout:` path with POST method
    - `openapi.yaml` does NOT contain `/auth/rso/authorize` (old wrong path removed)
    - `openapi.yaml` contains `ErrorResponse` schema with `error: string` property
    - `openapi.yaml` contains `isRedacted` field in match-related schema
    - `openapi.yaml` contains `_masked` field in match-player schema
    - `openapi.yaml` contains `isPrivate` field in player-profile schema
    - `openapi.yaml` contains `PaginatedPlayers` schema with `data`, `total`, `page`, `totalPages`
    - `openapi.yaml` contains `PaginatedMatches` schema with `data`, `total`, `page`, `totalPages`
    - GET `/players` path references `PaginatedPlayers` in 200 response
    - GET `/matches` path references `PaginatedMatches` in 200 response
    - GET `/players` has `page` and `limit` query parameters
    - GET `/matches` has `page` and `limit` query parameters
  </acceptance_criteria>
  <done>OpenAPI spec accurately documents all 7 auth endpoints, includes privacy response fields, uses paginated response wrappers for players and matches, and defines a unified ErrorResponse schema.</done>
</task>

<task type="auto">
  <name>Task 2: Run Orval codegen and verify zero errors (D-05)</name>
  <files>
    lib/api-client-react/src/generated/api.ts
    lib/api-client-react/src/generated/api.schemas.ts
    lib/api-zod/src/generated/api.ts
  </files>
  <read_first>
    lib/api-spec/orval.config.ts
    lib/api-spec/package.json
  </read_first>
  <action>
    Run the Orval codegen command from the `lib/api-spec` directory:

    ```bash
    cd lib/api-spec && pnpm run codegen
    ```

    This regenerates:
    - `lib/api-client-react/src/generated/api.ts` (React Query hooks)
    - `lib/api-client-react/src/generated/api.schemas.ts` (TypeScript types)
    - `lib/api-zod/src/generated/api.ts` (Zod validators)

    After codegen completes, verify:
    1. The generated files exist and are non-empty
    2. Run TypeScript type checking on the generated output:
       ```bash
       cd lib/api-client-react && npx tsc --noEmit 2>&1 | head -50
       ```
       If type errors occur, they indicate spec issues -- go back and fix the spec.
    3. Verify the new hooks exist for auth endpoints:
       ```bash
       grep "authDiscord\|authLogout\|authConnectToken" lib/api-client-react/src/generated/api.ts
       ```
    4. Verify paginated response types exist:
       ```bash
       grep "PaginatedPlayers\|PaginatedMatches" lib/api-client-react/src/generated/api.schemas.ts
       ```

    If codegen fails with YAML parse errors, fix the openapi.yaml syntax and re-run. Common issues:
    - Incorrect indentation (YAML is 2-space indent)
    - Missing required fields in schema definitions
    - Broken `$ref` references

    If TypeScript errors occur in generated code, the spec has schema issues. Fix the spec, re-run codegen. Do NOT edit generated files directly -- they are overwritten on every codegen run.

    The generated files are never edited by hand (they have a "DO NOT EDIT" header). Just commit them as-is after successful codegen.
  </action>
  <verify>
    <automated>test -s lib/api-client-react/src/generated/api.ts && test -s lib/api-zod/src/generated/api.ts && grep -c "authDiscord\|authLogout" lib/api-client-react/src/generated/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/api-client-react/src/generated/api.ts` exists and is non-empty (codegen ran)
    - `lib/api-client-react/src/generated/api.schemas.ts` exists and is non-empty
    - `lib/api-zod/src/generated/api.ts` exists and is non-empty
    - Generated api.ts contains hooks for `authDiscord` and `authLogout` (new auth endpoints)
    - Generated api.schemas.ts contains `PaginatedPlayers` type (or equivalent from spec schema name)
    - Generated api.schemas.ts contains `PaginatedMatches` type
    - `npx tsc --noEmit` in lib/api-client-react exits with 0 or the only errors are from the consuming frontend (not from generated code itself)
  </acceptance_criteria>
  <done>Codegen produces updated frontend hooks and Zod validators from the aligned OpenAPI spec. New auth hooks exist. Paginated response types exist. Zero type errors in generated code.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `openapi.yaml` documents all 7 auth routes with correct HTTP methods and paths
2. `openapi.yaml` includes ErrorResponse, isRedacted, _masked, isPrivate, PaginatedPlayers, PaginatedMatches
3. Generated hooks exist for all new auth endpoints
4. Generated types include paginated wrappers
5. No manual edits to generated files
</verification>

<success_criteria>
- OpenAPI spec is fully aligned with actual routes (zero spec drift)
- Auth endpoints documented: /auth/discord, /auth/discord/callback, /auth/connect/{token}, /auth/rso, /auth/rso/callback, /auth/me, /auth/logout
- Privacy fields documented: isRedacted on matches, _masked on match players, isPrivate on player profiles
- Paginated wrappers documented: PaginatedPlayers for GET /players, PaginatedMatches for GET /matches
- Codegen runs successfully and produces non-empty generated files
- Frontend can import the new hooks and types without modification
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-contract-performance/03-03-SUMMARY.md`
</output>
